const Discord = require("discord.js");
const fs = require('fs')

module.exports.run = async (client, msg) => {

	/*msg.delete();
	if (!client.dbl.hasVoted(msg.author.id)) return client.users.get(msg.author.id).send('Tu as déjà voté, reviens plus tard')
	client.users.get(msg.author.id).send('Tu n\'as pas encore voté !, voici votre occasion pour le faire: https://discordbots.org/bot/576809974697820188/vote')*/

}

module.exports.help = {
	name : "vote",
	alliase: [],
    type: "bot",
}
